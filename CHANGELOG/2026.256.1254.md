# 2026.256.1254 — refactor(distros): remove the duplicate omarchy-eval skills (owned by eval-omarchy)

## Summary

Removes the two hand-authored `omarchy-eval*` skill entities from this repo. The omarchy PR-eval procedure and the golden runbook now live as `skill:` entities in **opencharly/eval-omarchy** (`candy/eval-lane/charly.yml`, `family: distros`, `owner: eval-omarchy`) — which have ALREADY landed on eval-omarchy `origin/main` (tag `v2026.256.1245`) — and are generated into the marketplace from there. Keeping a second hand-authored copy here is an R3 one-owner violation; it had drifted (still naming the deleted `.agents/skills/` and the retired `eval/eval-pr.py`).

- `charly.yml` — delete `omarchy-eval-skill:` (name `omarchy-eval`) and `omarchy-eval-golden-skill:` (name `omarchy-eval-golden`); the successor entities are live in eval-omarchy.
- `charly.yml` — schema stamp `2026.232.0520` → `2026.249.2125` (the toolchain's required schema) so `charly box validate` runs.

## How tested

Change class: config (skill-entity deletion) → the repo's `charly box validate` gate + the live-owner liveness check + the R5 sweep, all on the final tree at head `65fa331`.

**1. The successor entities are LIVE on the owning repo's `origin/main`:**

```
$ git -C <eval-omarchy> show origin/main:candy/eval-lane/charly.yml | grep -nE 'name: omarchy-eval'
13:        name: omarchy-eval
54:        name: omarchy-eval-oracle
90:        name: omarchy-eval-tiers
115:        name: omarchy-eval-tests
216:        name: omarchy-eval-full-loop
243:        name: omarchy-eval-cold-reader
271:        name: omarchy-eval-golden
302:        name: omarchy-eval-media
322:        name: omarchy-eval-sequencing
338:        name: omarchy-eval-work-lane
```

**2. `charly box validate` (final tree):**

```
$ charly box validate
charly box validate: OK — checked 1 candy, 7 distros, 7 builders; 0 warnings, 0 errors
```

**3. R5 / B14(d) — repo-wide sweep over the skill NAMES and ALL file types** (excluding historical CHANGELOG):

```
$ grep -rn 'omarchy-eval' . 2>/dev/null | grep -v '^\./\.git/' | grep -v '/CHANGELOG/'
(no output — no surviving reference in charly.yml, README, docs, hooks, or any other file)
```

The earlier narrower key-only grep (`omarchy-eval-skill`) is superseded by this name-and-all-types sweep. The two removed entities were the ONLY referrers of the names in this repo (they cross-referenced each other), so the deletion leaves no dangling `/charly-distros:omarchy-eval*` reference here.

**Head diff (verbatim, at head `65fa331`):**

```
$ git diff origin/main...HEAD --stat
 charly.yml | 233 +------------------------------------------------------------
 1 file changed, 1 insertion(+), 232 deletions(-)
```

## Rulebook compliance

- **R1** — zero warnings / zero failures: `charly box validate` is clean; the liveness precondition (successor on `origin/main`) is checked and pasted.
- **R2 (atomic cutover / no deferral)** — the successor is ALREADY merged and tagged on eval-omarchy (`v2026.256.1245`), so this removal is atomic at the org level: the owner exists before the duplicate is deleted. No named follow-up is required.
- **R3** — one owner per skill: eval-omarchy owns the `omarchy-eval*` corpus.
- **R4** — no workaround; the duplicate is deleted, not stubbed.
- **R5** — the duplicate path is removed completely; the repo-wide name sweep is pasted.
- **R6** — `git status` clean; PR-only landing, no force-push.
- **R7** — `charly box validate` run on the final tree and pasted.
- **R8** — N/A — no OCI labels / Containerfile generation touched.
- **R9** — N/A — no binary/plugin rebuild; the change is a `charly.yml` entity deletion.
- **R10** — the change-class gate for a skill-entity deletion with no composing bed is `charly box validate` + the R5 sweep (both run here); no bed features this repo's skill corpus, so no bed can fail on the deletion.

## Change classification

- **Change class:** config (skill-entity removal).
- **Verification gate:** `charly box validate` (0 warnings / 0 errors) + the live-owner liveness check + the repo-wide name sweep, on the final tree at head `65fa331`.
- **Attribution tier:** fully tested and validated.

*Assisted-by: opencode opencode-go/deepseek-v4.1-flash (fully tested and validated)*