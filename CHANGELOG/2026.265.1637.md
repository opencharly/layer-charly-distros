# 2026.265.1637 — refactor(distros): drop the arch-family skills (moved to distro-arch)

## Summary

Remove the five arch-family `skill:` entities from this repo — they now live in
**opencharly/distro-arch** (merged + tagged `v2026.265.1557` by
`opencharly/distro-arch#39`) and are generated into the marketplace from there.
A second copy here is an R3 one-owner violation.

`charly.yml` — delete `arch-skill:` (`arch`), `arch-builder-skill:`,
`arch-aur-test-skill:`, `arch-pac-test-skill:`, `arch-test-skill:`. Plus this
repo's `CHANGELOG/2026.265.1606.md`.

The other two arch-family skills (`arch-coder`, `charly-arch`) were owned by
`layer-charly-coder` and move in that repo's companion PR, not here.

Files changed (`git diff --stat origin/main`):

```
 CHANGELOG/2026.265.1606.md |  81 +++++++++++
 charly.yml                 | 355 ---------------------------------------------
 2 files changed, 81 insertions(+), 355 deletions(-)
```

## How tested

Change class: config (skill-entity deletion).

**1. The successor entities are LIVE on the owning repo at the pinned tag:**

```
$ git -C distro-arch show v2026.265.1557:charly.yml | grep -c '^    skill:$'
7
$ git -C distro-arch show v2026.265.1557:charly.yml | grep -E '^(arch|arch-aur-test|arch-builder|arch-pac-test|arch-test)-skill:'
arch-aur-test-skill:
arch-builder-skill:
arch-pac-test-skill:
arch-test-skill:
arch-skill:
```

**2. `charly box validate` on this branch (post-deletion):**

```
$ charly box validate
charly box validate: OK — checked 1 candy, 7 distros, 7 builders; 0 warnings, 0 errors
```

**3. The five entities are gone and the remaining 22 skills are untouched:**

```
$ grep -c '^    skill:$' charly.yml
22
$ grep -E '^(arch|arch-aur-test|arch-builder|arch-pac-test|arch-test)-skill:' charly.yml
exit=1   # no matches
```

## Wave / landing order

1. `opencharly/distro-arch#39` — MERGED, tagged `v2026.265.1557`.
2. `opencharly/marketplace` — pins the source (regeneration no-op).
3. **this PR** — activation: the five skills now emit from a single source.

This PR is opened before step 2 merges so the reviewer sees the full wave; it must
NOT be merged before step 2, or the generation closure would lose the five
skills (they would be defined only in distro-arch, which is not yet pinned).

## Rulebook compliance

- **R0** — `/charly-internals:skills` + `/charly-internals:marketplace` loaded.
- **R1** — deletion is the counterpart of a completed relocation; no divergence.
- **R2** — one coherent cutover (steps 1–3); this is its final step.
- **R3** — one owner per skill after this landing.
- **R4** — no sleeps/retries/workarounds; `charly box validate` is the gate.
- **R5** — deprecated copies deleted (not shimmed) in the same wave as the move.
- **R6** — dedicated checkout, PR-only, no force-push.
- **R7** — `charly box validate` ran on this branch.
- **R8/R9** — no labels/plans/schemas; no binary built here.
- **R10** — config class; the marketplace drift gate in step 2 proves byte-identity.

## Change classification

Config (skill-entity deletion) → `analysed on a live system`. Proof: the owner at
`v2026.265.1557` carries all five entities; `charly box validate` clean; the five
nodes absent from this file.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*