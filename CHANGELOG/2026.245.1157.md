# 2026.245.1157 — docs(skill): sync vendored omarchy-eval skill to distro-omarchy #25

## Summary of changes

Sync the **vendored copy** of the `omarchy-eval-skill` candy entity in `layer-charly-distros/charly.yml` to the distro-omarchy main content after opencharly/distro-omarchy#25 (tag v2026.245.1103). This layer is how the marketplace corpus projects the omarchy-eval skill (marketplace refs pin `layer-charly-distros:v2026.244.1915`), so the guidance update must land here before the marketplace bump + regen.

- `content:` block replaced with the post-#25 procedure: evaluate from `opencharly/eval-omarchy`'s `eval/PR-EVAL-TEMPLATE.md` (EXTERNAL, NON-AUTHORITATIVE disclaimer; channel base stable/rc/edge/dev selection), the apply contract (SHA-pinned `pull/<N>/head`, changed-files-only, `pr-applied` proof), the snapshot lane (`snapshot: on_finalize: golden` bases, `libvirt: snapshot/revert`, `charly check live`, base provenance + stale-verdict warning), Tier-0/venue/evidence per the template, and the new Update-channel machinery section (channel-set round-trip incl. re-set idempotence, per-channel migrations, pacman `-Syu` guard, dev-channel `~/omarchy` checkout).
- Dangling refs fixed (no `§` markers); rollup path now names eval-omarchy as its home.
- Content is byte-identical to the source candy (YAML-parse verified); the node's other fields (`description`, `triggers`) are preserved; the block marker aligned to `|-` (strip) matching the source so the projected value matches exactly.

## Change class

**candy/box-config** — a vendored skill entity content edit; zero plan steps, zero bed changes, zero Go/schema.

**Closes / relates to:** syncs opencharly/distro-omarchy#25 into the corpus layer; prerequisite for the marketplace refs bump + regen (opencharly/marketplace)

**Breaking change + rollback:** none — guidance text; revert restores the previous content.

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this change class:** YAML validation of the edited candy + content parity with the source (a text-sync; no runtime surface changes).

```
$ python3 -c "import yaml; yaml.safe_load(open('charly.yml')); print('YAML OK')"
YAML OK

# content parity with the source candy (parsed content: values)
$ python3 /tmp/cmp3.py
ends: 'acts.' 'acts.'
len 5818 5818
# (content IDENTICAL)

$ git diff --stat
 charly.yml | 64 ++++-----------
 1 file changed, 42 insertions(+), 22 deletions(-)
```

- **Fresh rebuild / changed code path executing:** N/A — guidance text in a skill entity; no plan steps, no beds.
- **Concurrency:** N/A — no shared-state surface.

**Evidence blocks:**

*1. Fresh-rebuild gate output*: `N/A — no binary; text-only sync.`

*2. Each changed code path executing*: `N/A — no runtime code path changed.`

*3. Docs-only evidence*: YAML OK + content parity output above; diff inspected — only the `content:` block of the omarchy-eval-skill node changed (triggers/description intact).

## Attribution tier

**documentation reviewed** — the whole diff is guidance text inside a skill entity's `content:` block.

## Harness rulebook compliance — state HOW each is satisfied (or `N/A — <reason>`)

- **R0 skills:** git-workflow + marketplace skills loaded; the refs→projection chain verified before editing (this layer is the corpus source for the skill).
- **R1 RCA + zero warnings:** no failures/warnings; previous dangling-§ refs swept (grep 0).
- **R2 no out-of-scope:** content sync only; the marketplace bump + regen are the named follow-on (opencharly/marketplace PR, same change family).
- **R3 no duplication:** this layer IS the canonical vendored source for the corpus; no second copy edited.
- **R4 no workaround:** plain text sync of the owner repo's content.
- **R5 hard cutover + grep:** dangling refs removed; `grep -c '§' charly.yml` = 0.
- **R6 git safety:** status clean before branch/commit; no destructive ops.
- **R7 runtime gate:** text-only — YAML parse is the gate.
- **R8 artifact invariants:** N/A — corpus regeneration happens in the marketplace repo (drift gate) after the layer tag lands.
- **R9 binary == source + deps:** N/A — no binary.
- **R10 disposable + coverage:** N/A — no disposable target, no coverage delta.
- **RDD / ADE / SDD:** N/A — skill text; ADE invariants untouched.
- **Hard cutover:** one atomic commit; the marketplace bump is the named next step.
- **Kernel/plugin boundary law:** N/A.
- **Disposable-only autonomy:** N/A.
- **Clean architecture + Go gates:** N/A — no Go.
- **CHANGELOG:** N/A — tag-on-merge writes it from this PR body.

---

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (documentation reviewed)*