# 2026.265.1959 — refactor: distro-omarchy owns its distros skills (drop them from layer-charly-distros)

## Summary

`distro-omarchy` now owns its 2 skills (moved there by its producer PR in this wave, tagged `v2026.265.1655`). Remove the duplicate copies from this repo so each skill has exactly one owner (R3).

Deleted from `charly.yml`: `omarchy-cstream, omarchy`.

This is one coherent cutover: "stop owning the `distro-omarchy`-family skills" — a semantic unit
(the skills whose owner repo is `distro-omarchy`), not a size-based slice.

## How tested

**1. `charly box validate` on this branch (post-deletion), with a NEGATIVE CONTROL
proving it genuinely loads/validates skill entities** (breaking a REMAINING skill's
`name:` makes validate FAIL):

```
$ charly box validate
charly box validate: OK — checked 1 candy, 7 distros, 7 builders; 0 warnings, 0 errors

$ # negative control: delete `name:` from a remaining skill entity
$ charly box validate
charly: error: command "validate": validation error: …/charly.yml:doc0: node "...":
  plugin kind:skill: plugin_input fails #SkillInput: #SkillInput.name: incomplete value =~"^[a-z][a-z0-9-]*$"
```

- **Disposable target:** N/A — config class; no image/deploy created or changed.
- **Fresh-rebuild confirmation:** N/A — config class; this repo builds no binary.

**2. Successor liveness — each deleted entity is present on `distro-omarchy` at `v2026.265.1655` and its
`family:` is unchanged (so its `/charly-<family>:<skill>` reference still resolves):**

```
$ git -C distro-omarchy show v2026.265.1655:charly.yml | grep -A2 'name: omarchy-cstream$'
        name: omarchy-cstream
        family: distros
        owner: omarchy-cstream

$ git -C distro-omarchy show v2026.265.1655:charly.yml | grep -A2 'name: omarchy$'
        name: omarchy
        family: distros
        owner: omarchy
```

**3. The generation closure carries these skills from `distro-omarchy` — the cross-repo R10
proof.** `opencharly/marketplace#325` (MERGED) ran the generator over a closure
with the aggregator sources REMOVED and their moved skills deleted — the exact
post-deletion state — and all 37 wave skills still emitted from the distro-* pins
(37/37, 0 missing, 0 differing). Merging this after #325 cannot lose a skill.

## Wave / landing order

1. Producer PRs — MERGED + tagged.
2. `opencharly/marketplace#325` — pins the owner repos (MERGED).
3. **this PR** — activation for the `distro-omarchy`-family skills.

## Rulebook compliance

- **R0** — `/charly-internals:skills` + `/charly-internals:marketplace` loaded.
- **R1** — deletion is the counterpart of a completed relocation; no divergence.
- **R2** — one coherent wave; this is its activation step.
- **R3** — one owner per skill after this landing.
- **R4** — no sleeps/retries/workarounds; `charly box validate` is the gate.
- **R5** — deprecated copies deleted (not shimmed) in the same wave.
- **R6** — dedicated checkout, PR-only, no force-push.
- **R7** — validate (with negative control) + successor liveness ran (real output above).
- **R8/R9** — no labels/plans/schemas; no binary built here.
- **R10** — config class; the #325 generation closure run is the cross-repo proof.

## Change classification

Config (skill-entity deletion) → `analysed on a live system`. Proof: `distro-omarchy` carries
each deleted entity at `v2026.265.1655` (pasted); validate clean + negative control; closure
generation run proves the corpus is unaffected.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*