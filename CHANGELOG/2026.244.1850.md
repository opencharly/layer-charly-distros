# 2026.244.1850 — feat(distros): the omarchy-eval skill — the omarchy PR evaluation procedure

## Summary

Adds the `omarchy-eval` skill to the distros family — the charly-driven omarchy PR evaluation procedure. The skill was authored in `opencharly/distro-omarchy` (box/omarchy-suite-base/candy/omarchy-eval-skill, distro-omarchy#21) but distro-omarchy is not a refs-list repo, so the skill never reaches the marketplace corpus. This projects it into the distros family owner (this repo), matching the established pattern (the `omarchy` base-image skill lives here; `omarchy-vm` lives in vm-charly-vm).

Covers: the five evaluation beds (check-omarchy-suite-pod, -pr9343, -pr-vm, -pr-vm-visual, -hybrid-gpu-vm), the VFIO GPU passthrough lane (host readiness, auto-allocation, backend/firmware, in-guest driver, arbiter flips), the 7-step evaluation procedure, and the doctrine guardrails.

## How tested

- `python3 -c "import yaml; yaml.safe_load(open('charly.yml'))"` — YAML OK
- Entity structure verified: name=omarchy-eval, family=distros, owner=omarchy-eval, triggers present, content 4477 chars
- Content is a verbatim projection of the distro-omarchy source (distro-omarchy#21, merged)

## Rulebook compliance

- R3: no duplication — the skill entity lives ONLY here (the corpus owner); the distro-omarchy copy is the bed-side source
- R6: git safety — feat/ branch, no force-push, no direct main push
- PR-only landing per the org git-workflow

## Change classification

- Change class: docs/skill (no code, no schema)
- Verification gate: syntax check + YAML parse (the marketplace drift gate will regenerate and compare)
- Attribution tier: documentation reviewed

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*