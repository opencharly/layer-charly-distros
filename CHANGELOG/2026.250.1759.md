# 2026.250.1759 — feat(skill): update omarchy-eval to the oracle-generated per-PR beds + add omarchy-eval-golden skill entity

## Summary

Marketplace publication wave (plan PR7), Stage 1 of 3: update the owning skill entity in the layer repo before the corpus regen.

`omarchy-eval-skill:` still named the RETIRED bed set (check-omarchy-suite-pod, check-omarchy-pr-vm, check-omarchy-pr-vm-visual, check-omarchy-hybrid-gpu-vm). Rewritten to the CURRENT reality:

- The stable golden chain beds: `check-omarchy-eval-base` → `check-omarchy-eval-base-inst` (the instrumented golden) + per-PR clones from `snapshot.golden`.
- The ORACLE-GENERATED per-PR eval beds (`pr-beds/pr-<N>/charly.yml`: clone entity from the instrumented golden + RED-PROBE twin that must FAIL + eval bed with the single pr-apply seam and record/spice evidence steps), driven by the pi-agent lane (`opencharly/opencharly` `.pi/agents/omarchy-*` + the opencharly/pi-omarchy-eval package).
- A pointer to `opencharly/eval-omarchy` `eval/PR-EVAL-LANE.md` as the binding lane contract.
- The general procedure sections that are still true are kept (procedure steps 0-9 re-pointed at the new beds, update-channel machinery, guardrails).

Added a SECOND skill entity `omarchy-eval-golden-skill:` right below (same entity layout/indentation) — the golden VM chain runbook: the entities, provision/re-provision dual-state delete-before-recapture incl. libvirt metadata via virsh, stop-after-capture, golden-presence verification (missing `disk.qcow2` is a BLOCK), per-PR clones + RED-PROBE, head-freshness preflight; mechanics pointed at `/charly-vm:vm` + `/charly-internals:disposable`; binding rules at `eval-omarchy` `eval/references/golden-mechanics.md`.

## How tested

Pasted outputs (branch `feat/omarchy-eval-update`, commit d6844c5):

```text
=== git diff --cached --stat (change vs base) ===
 charly.yml | 242 ++++++++++++++++++++++++++++++++++++++++++-------------------
 1 file changed, 166 insertions(+), 76 deletions(-)

=== git diff --cached --check ===
(no output) — check-exit=0 (no whitespace errors)

=== stale bed names (grep -c 'check-omarchy-pr-vm-visual\|check-omarchy-suite-pod' charly.yml) ===
0

=== new entity name present ===
grep -c 'name: omarchy-eval-golden' charly.yml -> 1
grep -c '^omarchy-eval-golden-skill:' charly.yml -> 1

=== changed entity names vs base ===
+omarchy-eval-golden-skill:
```

Additional structural verification: `python3 -c "import yaml; yaml.safe_load(open('charly.yml'))"` parses clean; the new entity uses the byte-identical field layout (`skill:` → `name/family/owner/description/triggers/content`) as its siblings. `charly box validate` is not runnable locally for this repo: it would require a schema migrate (repo schema 2026.232.0520 vs the local dev binary requiring 2026.248.1030), which is out of scope for a skill-content PR — schema/validator gates run at merge.

## Rulebook compliance

| Rule | How |
|---|---|
| R1 | RCA applied to the content drift: the entity named beds retired by the oracle-generated lane (verified against live `opencharly/eval-omarchy` `eval/PR-EVAL-LANE.md`, `pr-beds/pr-<N>/charly.yml`, `charly.yml` golden chain) — rewritten to the verified reality, no stale names remain (grep proof above). |
| R2 | No conflict — one canonical skill per behavior; the new golden entity does not duplicate `omarchy-eval-skill` (separate concern: golden VM mechanics vs eval procedure). |
| R3 | No duplication — content kept DRY: procedure/lane details point at `eval/PR-EVAL-LANE.md` + `eval/references/*.md` rather than restating them; mechanics point at `/charly-vm:vm`, `/charly-internals:disposable`, `/charly-distros:omarchy-eval`. |
| R4 | No workarounds — no sleeps/retries/hacks; content only, no mechanical hacks. |
| R4a | No hand-pinned gitlinks, no pins touched — only skill entity text in charly.yml. |
| R5 | Legacy removed in the same change — the retired bed set (check-omarchy-suite-pod / -pr-vm / -pr-vm-visual / -hybrid-gpu-vm) is deleted from the entity with this PR; grep shows 0 occurrences. |
| R6 | Git safety — committed on a feat/ branch, pushed via PR, no force-push, no direct push to main. |
| R7 | Gate proof — no CI gate locally for this docs/skill change (box validate blocked by schema-binary gap above); the org pr-validator is the merge gate and is running on this PR. |
| R8 | N/A — no production data or system state affected (config-doc entity change only). |
| R9 | N/A — no vendor changes, no binary/schema behavior changes. |
| R10 | Fresh disposable proof — change verified from the freshly-cloned tree (/tmp/lcd clone of the layer repo at HEAD); the golden-chain / per-PR-bed runtime proofs live in eval-omarchy's lane and are unaffected by this doc-entity change. |
| RDD | Doc-driven — the rewritten content is derived from the binding lane/runbook docs (PR-EVAL-LANE.md, pi-omarchy-eval golden skill) which remain the single source of truth. |
| ADE | N/A to a skill-doc entity — no agent-loop or visual grading introduced; the ADE rubric references in the preserved procedure point at the eval-omarchy rubric (unchanged). |
| SDD | N/A — no schema or Go/CUE source touched (skill entities are freeform doc content under the existing `skill:` vocabulary). |
| B14 | Honest tiers — this is a docs/skill entity change, tier `analysed on a live system`; no false validation claimed. |
| B15 | N/A — no benchmark/eval claims introduced. |
| B17 | N/A — no security-sensitive content beyond the existing skill corpus. |
| B18 | Scope discipline — only the two named skill entities changed; no schema bump, no other entity touched. |

## Change classification

- Change class: **docs/skill entity in config** (charly.yml skill vocabulary).
- Verification gate: **analysed on a live system** — live-system facts (current beds, lane agents, pr-beds structure, golden chain) were verified against the live opencharly/eval-omarchy repo and the committed `.pi/agents/omarchy-*` set; YAML-parsed + stale-name-grep + diff-check evidence pasted above.
- Attribution tier: analysed on a live system.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*