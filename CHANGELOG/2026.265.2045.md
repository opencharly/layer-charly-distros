# 2026.265.2045 — refactor: distro-ubuntu owns its distros skills (drop them from layer-charly-distros)

## Summary

`distro-ubuntu` now owns its 4 skills (moved there by its producer PR in this wave, tagged `v2026.265.1657`). Remove the duplicate copies from this repo so each skill has exactly one owner (R3).

Deleted from `charly.yml`: `ubuntu-builder, ubuntu-debootstrap-builder, ubuntu-debootstrap, ubuntu`.

This is one coherent cutover: "stop owning the `distro-ubuntu`-family skills" — a semantic unit
(the skills whose owner repo is `distro-ubuntu`), not a size-based slice.

## How tested

**1. `charly box validate` on this branch (post-deletion), with a NEGATIVE CONTROL
proving it genuinely loads/validates skill entities** (breaking a REMAINING skill's
`name:` makes validate FAIL):

```
$ charly box validate
charly box validate: OK — checked 1 candy, 7 distros, 7 builders; 0 warnings, 0 errors

$ # negative control: delete `name:` from a remaining skill entity
$ charly box validate
charly: error: command "validate": validation error: …/charly.yml:doc0: node "...":
  plugin kind:skill: plugin_input fails #SkillInput: #SkillInput.name: incomplete value =~"^[a-z][a-z0-9-]*$"
```

- **Disposable target:** N/A — config class; no image/deploy created or changed.
- **Fresh-rebuild confirmation:** N/A — config class; this repo builds no binary.

**2. Successor liveness — each deleted entity is present on `distro-ubuntu` at `v2026.265.1657` and its
`family:` is unchanged (so its `/charly-<family>:<skill>` reference still resolves):**

```
$ git -C distro-ubuntu show v2026.265.1657:charly.yml | sed -n '185,187p'
        name: ubuntu-builder
        family: distros
        owner: ubuntu-builder

$ git -C distro-ubuntu show v2026.265.1657:charly.yml | sed -n '515,517p'
        name: ubuntu-debootstrap-builder
        family: distros
        owner: ubuntu-debootstrap-builder

$ git -C distro-ubuntu show v2026.265.1657:charly.yml | sed -n '566,568p'
        name: ubuntu-debootstrap
        family: distros
        owner: ubuntu-debootstrap

$ git -C distro-ubuntu show v2026.265.1657:charly.yml | sed -n '625,627p'
        name: ubuntu
        family: distros
        owner: ubuntu
```

**3. The generation closure carries these skills from their new owners — the
cross-repo R10 proof (real, executed output).** `opencharly/marketplace#325`
(MERGED) ran the generator over a closure with the nine aggregator sources
REMOVED and their moved skills deleted (git `insteadOf` to locally-patched
copies) — the exact post-deletion state — and all 37 wave skills still emitted:

```
$ charly marketplace generate --root proj-closure --out closure-out3
EXIT=0
wave skills present in closure-WITHOUT-aggregators: 37/37
missing: []
content-differs: []
```

Merging this after #325 cannot lose a skill. (The generator's own resolution rule
— `candy/plugin-marketplace` `resolve.go:preferEntity`, highest source tag wins —
resolves each skill to its distro-* owner.)

**Authoring-runtime evidence (A3).** The footer names the runtime that authored
this PR. The harness reported its model as `deepseek-v4.1-flash` and its provider
route as `ollama-cloud` (model id `ollama-cloud/deepseek-v4.1-flash`), matching the
`*Assisted-by:*` line.

**4. R5 self-test — the deleted definitions are gone from this repo (executed
sweeps, both the entity key and the `name:` definition line):**

```
$ grep -rn -E '(ubuntu-builder-skill:|ubuntu-debootstrap-builder-skill:|ubuntu-debootstrap-skill:|ubuntu-skill:)' . --exclude-dir=.git
(no matches)
# exit=1

$ grep -rn -E '^ +name: (ubuntu-builder|ubuntu-debootstrap-builder|ubuntu-debootstrap|ubuntu)$' . --exclude-dir=.git
(no matches)
# exit=1
```

## Wave / landing order

1. Producer PRs — MERGED + tagged.
2. `opencharly/marketplace#325` — pins the owner repos (MERGED).
3. **this PR** — activation for the `distro-ubuntu`-family skills.

## Rulebook compliance

- **R0** — `/charly-internals:skills` + `/charly-internals:marketplace` loaded.
- **R1** — deletion is the counterpart of a completed relocation; no divergence.
- **R2** — one coherent wave; this is its activation step.
- **R3** — one owner per skill after this landing.
- **R4** — no sleeps/retries/workarounds; `charly box validate` is the gate.
- **R5** — deprecated copies deleted (not shimmed); the proof-4 sweeps are the self-test.
- **R6** — dedicated checkout, PR-only, no force-push.
- **R7** — validate (with negative control) + successor liveness ran (real output above).
- **R8/R9** — no labels/plans/schemas; no binary built here.
- **R10** — config class; the #325 generation closure run is the cross-repo proof.

## Change classification

Config (skill-entity deletion) → `analysed on a live system`. Proof: `distro-ubuntu` carries
each deleted entity at `v2026.265.1657` (pasted); validate clean + negative control; closure
generation run proves the corpus is unaffected.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*