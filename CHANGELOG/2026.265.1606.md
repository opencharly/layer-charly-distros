# 2026.265.1606 — refactor(distros): drop the arch-family skills (moved to distro-arch)

## Summary

Removes the five arch-family `skill:` entities from this repo. They now live as
`skill:` entities in **opencharly/distro-arch** (moved there by
`opencharly/distro-arch#39`, MERGED and tagged `v2026.265.1557`) and are
generated into the marketplace from there. Keeping a second copy here is an R3
one-owner violation.

- `charly.yml` — delete `arch-skill:` (name `arch`), `arch-builder-skill:`,
  `arch-aur-test-skill:`, `arch-pac-test-skill:`, `arch-test-skill:`.
- The companion `opencharly/marketplace` PR
  (`chore(refs): pin distro-arch:v2026.265.1557 as a corpus source`) pins the new
  owner as a corpus source BEFORE this deletion lands, so the generation closure
  never loses the five skills.

The other two arch-family skills (`arch-coder`, `charly-arch`) were owned by
`layer-charly-coder` and move in that repo's companion PR, not here.

## How tested

Change class: config (skill-entity deletion) → the repo's `charly box validate`
gate, plus the owner-liveness check, on the final tree.

**1. The successor entities are LIVE on the owning repo at the pinned tag:**

```
$ git -C distro-arch show v2026.265.1557:charly.yml | grep -c '^    skill:$'
7
$ git -C distro-arch show v2026.265.1557:charly.yml | grep -E '^(arch|arch-aur-test|arch-builder|arch-pac-test|arch-test)-skill:'
arch-aur-test-skill:
arch-builder-skill:
arch-pac-test-skill:
arch-test-skill:
arch-skill:
```

**2. `charly box validate` on this branch (post-deletion):**

```
$ charly box validate
charly box validate: OK — checked 1 candy, 7 distros, 7 builders; 0 warnings, 0 errors
```

**3. The five entities are gone from this file and the remaining 22 skills are
untouched:**

```
$ grep -c '^    skill:$' charly.yml
22
$ grep -E '^(arch|arch-aur-test|arch-builder|arch-pac-test|arch-test)-skill:' charly.yml
exit=1   # no matches
```

## Wave / landing order

1. `opencharly/distro-arch#39` — MERGED, tagged `v2026.265.1557`.
2. `opencharly/marketplace` — pins `distro-arch:v2026.265.1557` (regeneration no-op).
3. **this PR** — the activation step; the five skills now emit from a single source.

## Rulebook compliance

- **R1** — the deletion is the counterpart of a completed relocation; no divergence.
- **R2** — the wave is one coherent cutover (steps 1–3); this is its final step.
- **R3** — one owner per skill: after this landing the five entities exist only in `distro-arch`.
- **R4** — no sleeps/retries/workarounds; `charly box validate` is the gate.
- **R5** — the deprecated copies are deleted (not shimmed) in the same wave as the move.
- **R6** — dedicated checkout, PR-only, no force-push.
- **R7** — `charly box validate` ran on this branch.
- **R8/R9** — no labels/plans/schemas; no binary built here.
- **R10** — config change class (skill-entity deletion); the marketplace drift
  gate in step 2's PR proves the corpus is byte-identical.

## Change classification

Config (skill-entity deletion) → `analysed on a live system`. Proof: owner at
`v2026.265.1557` carries all five entities; `charly box validate` clean; the five
nodes absent from this file.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*
