# 2026.244.1915 — feat(distros): the omarchy-cstream skill — the streamed Omarchy desktop

## Summary

Adds the `omarchy-cstream` skill to the distros family — the streamed Omarchy desktop (Hyprland + Quickshell over HTTPS). This resolves the cross-reference `/charly-distros:omarchy-cstream` in the `omarchy` skill, which the docs generator's link gate rejects (the reference must resolve to a defined skill). The omarchy-cstream box is a real shipped box (distro-omarchy), so the skill is the right fix (R3 — resolve, not remove).

Covers: why cstream and NOT selkies (the wl_compositor v6 constraint), why it bases on omarchy rather than composing omarchy-base (the gstreamer dependency-order failure), and the verification surface.

## How tested

- `python3 -c "import yaml; yaml.safe_load(open('charly.yml'))"` — YAML OK
- Entity structure: name=omarchy-cstream, family=distros, owner=omarchy, triggers present
- The docs generator's cross-reference gate now resolves `/charly-distros:omarchy-cstream` (verified by re-running `charly docs generate` after the marketplace bump)

## Rulebook compliance

- R3: no duplication — the skill entity lives ONLY here (the corpus owner)
- R6: git safety — feat/ branch, no force-push, no direct main push
- PR-only landing per the org git-workflow

## Change classification

- Change class: docs/skill (no code, no schema)
- Verification gate: syntax check + YAML parse + the docs cross-reference gate
- Attribution tier: documentation reviewed

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*