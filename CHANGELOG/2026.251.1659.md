# 2026.251.1659 — docs(skill): omarchy-eval — CURRENT WORKFLOW section (driver, skills location, race FIXED, publication gate)

## Summary

The marketplace `distros/skills/omarchy-eval` projection is generated from this layer's `omarchy-eval-skill` candy content (the layer is the marketplace refs' projection vehicle for the omarchy-eval family — see the corpus git history: the family is regenerated from layer-charly-distros pins). This PR adds a **CURRENT WORKFLOW (2026-09-08)** section to the candy content:

- the ONE driver `eval/eval-pr.py` (eval-omarchy; eval.yml config, -d/--debug) executing the deterministic mechanics + dispatching the committed pi agents;
- the authoritative detailed lane guidance at eval-omarchy `.agents/skills/omarchy-eval-*` (the post-#41 standard location);
- the VM-BUILD OVERLAY WRITE-LOCK RACE **FIXED** note (plugin-vm#29 + charly#569, tag v2026.251.1204: idempotent clone-drive, read-only snapshot-store backing; proven 14/14 lanes, zero lock incidents);
- the publication gate (nothing posts to omacom/omarchy — operator-owned).

(Mirrors the distro-omarchy#48 owner-candy edit, merged + tagged v2026.251.1654.) After this merges + tags, the marketplace refs bump + `charly marketplace generate` carries the guidance into the published skill.

## How tested

```
$ grep -c 'CURRENT WORKFLOW' charly.yml
1
$ git diff --stat
 charly.yml | 13 +++++++++++++
 1 file changed, 13 insertions(+)
# python3 yaml.parse of the region around the omarchy-eval-skill node: ok (content block indented consistently)
# NOTE: `charly box validate` against the local fixed binary errors with a PRE-EXISTING schema
# mismatch (schema 2026.249.2125 required, found 2026.232.0520) — identical on the unmodified
# file (verified via git stash); an environment version skew, not caused by this change. The
# repo's validator uses its own pinned charly.
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R1 | The pre-existing schema skew is documented, not hidden; the change itself is content-only. |
| R2 | N/A. |
| R3 | The layer content is the single generation source for the marketplace omarchy-eval family. |
| R4 | N/A - documentation. |
| R4a | N/A. |
| R5 | N/A. |
| R6 | feat/ branch from origin/main; no force-push. |
| R7 | grep + diff pasted; the schema error proven pre-existing. |
| R8-R9 | N/A. |
| R10 | The regen after the tag is the merged proof. |
| RDD | N/A. |
| ADE | N/A. |
| SDD | N/A. |
| B14 | Outputs pasted. |
| B15-B18 | N/A. |

## Change classification

- Change class: docs (candy skill content, +13).
- Verification gate: grep + diff + yaml region parse; schema skew identified as pre-existing.
- Attribution tier: documentation reviewed.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*